//Sometimes, we wanna limit the values we can assign to a variable
//Let say we wanna assign value to a quantity either be 50 or 100 but nothing else
//So to limit values in a variable we use literal types
//Literal(exact, specific)

//Number
type Quantity = 50 | 100;    //Literal Type
let quantity: Quantity = 50;


//String
type Metric = 'cm' | 'inch';