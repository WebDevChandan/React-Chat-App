export const metadata = {
  title: 'Data Fetching',
  description: 'Fetching Data by API at Client Side & Server Side Component',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>
        {children}
      </body>
    </html>
  )
}
